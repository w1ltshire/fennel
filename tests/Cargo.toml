[package]
name = "tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
fennel-core = { path = "../crates/fennel-core" }
fennel-engine = { path = "../crates/fennel-engine" }
tokio = { version = "1.48.0", features = ["full"] }
